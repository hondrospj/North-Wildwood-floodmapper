name: Rebuild pre-2007 Lewes archive

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/update-lewes-archive.yml
      - tools/update_lewes_archive.py
      - tools/build_observed_archive_shards.py
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: north-wildwood-lewes-archive
  cancel-in-progress: false

jobs:
  rebuild-lewes-archive:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Pull verified NOAA Lewes hourly archive and six-minute gap fills
        run: python3 tools/update_lewes_archive.py

      - name: Rebuild browser-optimized archive indexes and year shards
        run: python3 tools/build_observed_archive_shards.py

      - name: Commit rebuilt archive when NOAA data changed
        shell: bash
        run: |
          if git diff --quiet -- lewes_hourly.json observed_archive_index.json lewes_archive_index.json observed_archive; then
            echo "Lewes archive is already current."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add lewes_hourly.json observed_archive_index.json lewes_archive_index.json observed_archive
          git commit -m "Rebuild pre-2007 Lewes archive"
          git pull --rebase --autostash origin main
          git push
